<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The window the OS paints before Flutter's first frame. AppColors.yellow,
         so the launch screen reads as the brand rather than as a blank window. -->
    <color name="launch_background">#FFFFD600</color>
</resources>
